# Changesets

This directory holds [changesets](https://github.com/changesets/changesets) - one Markdown file per change describing what was bumped and why.

To record a change:

```sh
pnpm changeset
```

The release workflow consumes these files to bump versions and publish to npm.
