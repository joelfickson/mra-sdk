// Re-exports the generated OpenAPI schema types as an escape hatch for
// consumers that need direct access to upstream DTO shapes.
export type { paths, components, operations } from './generated/schema.js';
